# ☕ Web3 Coffee Donation DApp

A decentralized application (DApp) that allows users to send ETH tips (donations) to a creator. Each donation is stored on the Ethereum blockchain as a permanent message.

## 🚀 Features
- **Smart Contract:** Written in Solidity, deployed on Sepolia Testnet.
- **Connect Wallet:** Integrated with MetaMask.
- **Immutable Messages:** Messages are stored forever on the blockchain.
- **Withdraw Funds:** Only the owner (creator) can withdraw the collected ETH.

## 🛠️ Tech Stack
- **Frontend:** React.js, Tailwind CSS
- **Blockchain:** Solidity, Hardhat, Ethers.js

## 📦 Installation
1. Clone the repo: `git clone <your-repo-url>`
2. Install dependencies: `npm install`
3. Start the project: `npm run dev`
